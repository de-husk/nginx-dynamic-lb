upstream servers {
    ${SERVERS}
}

server {
    listen 80;

    location / {
        proxy_pass http://servers;
        proxy_set_header Host ${HOST};
    }

    location /stub_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        deny all;
    }
}
